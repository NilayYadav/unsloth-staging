# Disposable repro workflow. Delete after validation.

name: "PR11297 resumed timing reproduction"

on:
  push:
    branches:
      - "pr-11297-repro-20260920-*"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  repro:
    name: "Targeted repro"
    runs-on: "macos-14"
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          persist-credentials: false

      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405
        with:
          python-version: "3.12"

      - name: "Assert resumed timing from shipped callback bodies"
        shell: bash
        run: |
          python tests/pr11297/repro.py

      - name: Preserve numerical evidence
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: resumed-training-facts
          path: repro-facts.json
